=====================================
``compat`` -- Backwards compatibility
=====================================

The ``compat`` module provides library functions necessary for Python 2.5
compatibility.

It is highly recommended to only import functions from this module on an
as-needed basis.

.. automodule:: bravo.compat
