=================================
``packets`` -- Packet serializers
=================================

The ``packets`` module contains descriptions for all packets in the wire
protocol, as well as several utility functions for encoding data to and
decoding data from packets.

.. automodule:: bravo.packets
